name: Epic Firka Builder Workflow

on: workflow_dispatch

jobs:
  build-android:
    name: Build for Android
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
      KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      KEY_ALIAS: ${{ secrets.KEY_ALIAS }}

    steps:
      - name: Download repository
        uses: actions/checkout@v4.2.2

      - name: Download Android keystore
        id: android_keystore
        if: ${{ env.KEYSTORE_BASE64 != '' && env.KEY_PASSWORD != '' && env.KEY_ALIAS != '' }}
        uses: timheuer/base64-to-file@v1.2.4
        with:
          fileName: firkaapp.jks
          encodedString: ${{ env.KEYSTORE_BASE64 }}

      - name: Create key.properties
        if: ${{ env.KEYSTORE_BASE64 != '' && env.KEY_PASSWORD != '' && env.KEY_ALIAS != '' }}
        run: |
          echo "storeFile=${{ steps.android_keystore.outputs.filePath }}" > refilc/android/key.properties
          echo "storePassword=${{ env.KEY_PASSWORD }}" >> refilc/android/key.properties
          echo "keyPassword=${{ env.KEY_PASSWORD }}" >> refilc/android/key.properties
          echo "keyAlias=${{ env.KEY_ALIAS }}" >> refilc/android/key.properties
          cp refilc/android/key.properties refilc/android/debugkey.properties

      - name: Keystore not configured (debug-signed release)
        if: ${{ env.KEYSTORE_BASE64 == '' || env.KEY_PASSWORD == '' || env.KEY_ALIAS == '' }}
        run: echo "Android signing secrets are missing, continuing with debug signing fallback."

      - uses: actions/setup-java@v4.7.0
        with:
          distribution: "temurin"
          java-version: "17"
          cache: "gradle"

      - uses: subosito/flutter-action@v2.18.0
        with:
          flutter-version: "3.27.1"
          channel: "stable"
          cache: "true"

      - name: Install dependencies
        run: |
          chmod +x fix-pub.sh
          ./fix-pub.sh

      - name: Build
        run: |
            cd refilc
            chmod +x build.sh
            ./build.sh
            cd ..

      - name: Upload Android Release
        uses: actions/upload-artifact@v4.6.1
        with:
          name: android-apks
          path: refilc/build/app/outputs/flutter-apk/app-*-release.apk


  build-ios:
    name: Build for IOS
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - name: Download repository
        uses: actions/checkout@v4.2.2

      - uses: subosito/flutter-action@v2.18.0
        with:
          flutter-version: "3.27.1"
          channel: "stable"
          cache: "true"

      - name: Install dependencies
        run: |
          chmod +x fix-pub.sh
          bash fix-pub.sh

      - name: Build
        run: |
          cd refilc
          chmod +x build-ipa.sh
          bash build-ipa.sh
          cd ..

      - name: Create IPA
        run: |
          cd refilc
          cd build/ios/archive/Runner.xcarchive/Products/Applications
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r Payload.zip Payload
          mv Payload.zip firkaapp.ipa
          cd ../../../../..

      - name: Upload IOS Release
        uses: actions/upload-artifact@v4.6.1
        with:
         name: ios-ipa
         # refilc/build/app/outputs/flutter-apk/app-*-release.apk
         path: refilc/build/ios/archive/Runner.xcarchive/Products/Applications/*.ipa
